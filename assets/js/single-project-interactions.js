/**
 * Single Project Page Interactions
 * Gestisce meta data, progress bar e sharing per progetti
 */


class SingleProjectManager {


}

// Initialize quando il DOM è pronto
document.addEventListener('DOMContentLoaded', () => {
	new SingleProjectManager();
});